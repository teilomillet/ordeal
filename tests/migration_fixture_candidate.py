"""Candidate module for the real-engine migration acceptance fixture."""

from __future__ import annotations


def increment(value: int) -> int:
    """Return the next integer."""
    return value + 1
